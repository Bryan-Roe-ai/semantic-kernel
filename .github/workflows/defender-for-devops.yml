name: "Microsoft Defender For DevOps"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '38 5 * * 5'

jobs:
  MSDO:
    # Only windows-latest is currently supported
    runs-on: windows-latest

    steps:
    # Check out the repository
    - name: Checkout repository
      uses: actions/checkout@v3.1.0
    
    # Set up .NET environment
    - name: Set up .NET
      uses: actions/setup-dotnet@v3.0.2
      with:
        dotnet-version: |
          5.0.x
          6.0.x

    # Run Microsoft Security DevOps
    - name: Run Microsoft Security DevOps
      uses: microsoft/security-devops-action@v1.10.0
      id: msdo

    # Upload results to Security tab
    - name: Upload results to Security tab
      uses: github/codeql-action/upload-sarif@v2.1.6
      with:
        sarif_file: ${{ steps.msdo.outputs.sarifFile }}

  # Optional: Add a job for Ubuntu (if supported in the future)
  # MSDO_Ubuntu:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Checkout repository
  #     uses: actions/checkout@v3.1.0
  #   - name: Set up .NET
  #     uses: actions/setup-dotnet@v3.0.2
  #     with:
  #       dotnet-version: |
  #         5.0.x
  #         6.0.x
  #   - name: Run Microsoft Security DevOps
  #     uses: microsoft/security-devops-action@v1.10.0
  #     id: msdo
  #   - name: Upload results to Security tab
  #     uses: github/codeql-action/upload-sarif@v2.1.6
  #     with:
  #       sarif_file: ${{ steps.msdo.outputs.sarifFile }}
