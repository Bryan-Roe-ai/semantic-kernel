name: Enhanced CI CD Pipeline (Minimal Diagnostic)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  minimal:
    runs-on: ubuntu-latest
    steps:
      - name: Show basic context
        run: |
          echo "Ref: $GITHUB_REF"
          echo "SHA: $GITHUB_SHA"
          echo "Actor: $GITHUB_ACTOR"
          echo "Event: $GITHUB_EVENT_NAME"
      - name: Success marker
        run: echo "Minimal workflow executed"
